[project]
name = "reboot2025"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "langchain>=1.2.0",
    "langchain-mistralai>=1.1.1",
]

[tool.uv.workspace]
members = [
    "database",
    "assessments",
]
